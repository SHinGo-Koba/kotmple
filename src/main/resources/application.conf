ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [com.example.ApplicationKt.module]
  }
  oauth2 {
    google {
      clientId = ${?GOOGLE_CLIENT_ID},
      clientSecret = ${?GOOGLE_CLIENT_SECRET},
    }
  }
}
